# Single encrypted key store

Linkmark 将 KDBX 密钥库作为唯一业务数据源：入口、密钥、分组、标签、排序和关联均在其中加密保存，锁定时不泄露任何业务元数据。入口拥有密钥关联的唯一事实来源；表单保存时由 Worker 生成 KDBX 密文，再与关联修改在 IndexedDB 中原子提交。此设计以解锁后才能浏览和搜索为代价，换取完全取消明文入口目录与可选密钥库之间的安全边界漏洞。
